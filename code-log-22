import tkinter as tk
from tkinter import ttk
import time
import datetime

# pink: #ff00e4 grün:#00ff00

root = tk.Tk()
root.title('title')
root.attributes('-topmost', True)
root.configure(pady=10, padx=10, background='#ff00e4')

#-------window settings:
swidth = root.winfo_screenwidth()
sheight = root.winfo_screenheight()

width = int(swidth * 0.25)
height = int(sheight * 0.39)

position_x = int((swidth - width)/2)
position_y = int((sheight - height)/2)

root.geometry(f'{width}x{height}+{position_x}+{position_y}')
#------------------------

button1 = ttk.Button(root, text='click')
#pixelbasierte platzierung, sehr statisch
button1.place(x=0, y=0, width=200, height = 200)

button2 = ttk.Button(root, text='click')
button2.place(x=210, y=0, width=200, height = 200)

button3 = ttk.Button(root, text='click')
button3.place(x=210, y=210, width=200, height = 200)

button4 = ttk.Button(root, text='click')
button4.place(x= 0, y= 210, width=200, height = 200)

#---------style settings:
style = ttk.Style()
style.theme_use('alt')

#-----------------------

#---------programm start:
root.mainloop()