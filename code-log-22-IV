import tkinter as tk
from tkinter import ttk
from PIL import Image, ImageTk
import random

# pink: #ff00e4 grün:#00ff00

root = tk.Tk()
root.title('title')
root.attributes('-topmost', True)
root.configure(pady=10, padx=10, background='#ff00e4')

#-------window settings:
swidth = root.winfo_screenwidth()
sheight = root.winfo_screenheight()

width = int(swidth * 0.25)
height = int(sheight * 0.39)

position_x = int((swidth - width)/2)
position_y = int((sheight - height)/2)

root.geometry(f'{width}x{height}+{position_x}+{position_y}')
#------------------------
#-----------image settings:
image = Image.open('assets/images/logo.png')
image.thumbnail((100,100))
photo = ImageTk.PhotoImage(image)
#----------------------

#-----------functions:

def show_surprise(event):
    if event.keysym == 'space':
        x = random.uniform(0, 0.9)
        y = random.uniform(0, 0.9)
        surpriselabel= ttk.Label(root, text='bye')
        surpriselabel.place(relx=x, rely=y)
        label1.lift()

#----------programm structure
label1 = ttk.Label(root, text='??????', anchor='center')
label1.place(relx=0.5, rely=0.5, relheight=0.1, relwidth=0.2, anchor='center' )


#eventstring: '<eventname>'
#label1.bind('<Configure>', show_surprise)

root.bind('<KeyPress>', show_surprise)


#---------style settings:
style = ttk.Style()
style.theme_use('alt')

#-----------------------

#---------programm start:
root.mainloop()
