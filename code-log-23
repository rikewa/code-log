import tkinter as tk 
from tkinter import ttk
import random

# pink: #ff00e4 grün:#00ff00

root = tk.Tk()
root.title('hello')
root.attributes('-topmost', True)
root.configure(background='#ff00e4')

coordinates = tk.StringVar()

def show_coordinates(event):
    coordinates.set(str(event.x_root) + ' ' + str(event.y_root))


#-----------functions:
def show_label(event):
    print('clicked at x=' + str(event.x) + ' y=' + str(event.y))

    x = random.uniform(0, 0.9)
    y = random.uniform(0, 0.9)

    showLabel = ttk.Label(root, text='HEY')
    showLabel.place(relx = x, rely = y)
    label1.lift()

#--------- window settings:
swidth = root.winfo_screenwidth()
sheight = root.winfo_screenheight()

width = int(swidth * 0.15)
height = int(sheight * 0.3)

position_x = int((swidth - width)/2)
position_y = int((sheight - height)/2)

root.geometry(f'{width}x{height}+{position_x}+{position_y}')
#----------------------------

#---------style settings:
style = ttk.Style()
style.theme_use('alt')

#------------------------

#-------------window structure:
label1= ttk.Label(root, text='hello this is text', anchor='center')
label1.place(relx=0.5, rely=0.5, relheight=0.15, relwidth=0.5, anchor='center')

#label1.bind('<Shift-Motion->', show_label)

label1.bind('<Double-Button-1>', show_label)


label2= ttk.Label(root, textvariable=coordinates, anchor='center')
label2.place(relx=0.5, rely=0.9, relheight=0.15, relwidth=0.5, anchor='center')

root.bind('<Motion>', show_coordinates)

#-------------------program start:
root.mainloop()
